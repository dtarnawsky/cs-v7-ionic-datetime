import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.v7.date.picker',
  appName: 'cs-v7-date-picker',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
